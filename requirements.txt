# ---------------------------------------------------------------------------
# Speech Understanding Assignment 2 -- Python dependencies
#
# Tested on:
#   - Python 3.10 / 3.11 (Windows + miniconda)
#   - Python 3.10 (Linux/CPU)
#
# CRITICAL: torch / torchaudio / torchvision MUST be the same major.minor.
# Mismatched versions produce cryptic OSError / "operator does not exist"
# crashes that are hard to debug. Install all three from PyTorch's index
# in one command (see README "Install" section).
# ---------------------------------------------------------------------------

# --- core PyTorch stack (versions must match exactly) ----------------------
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0

# --- numerical + audio I/O ------------------------------------------------
numpy>=1.26
scipy>=1.11
soundfile>=0.12
librosa>=0.10
matplotlib>=3.8

# --- speech models / Hugging Face -----------------------------------------
transformers>=4.40
huggingface_hub>=0.23
tokenizers>=0.15
safetensors>=0.4

# --- Part I: ASR support --------------------------------------------------
nltk>=3.8           # tokenisation for the trigram LM
pyctcdecode>=0.5    # (optional) reference impl of beam search w/ LM
jiwer>=3.0          # WER computation utilities

# --- Part II: G2P ---------------------------------------------------------
g2p_en>=2.1         # English grapheme-to-ARPA-BET
eng_to_ipa>=0.0.2   # ARPA-BET -> IPA fallback

# --- Part III: voice cloning ----------------------------------------------
# kNN-VC is loaded via torch.hub.load("bshall/knn-vc"), no pip dep.
# WavLM + HiFi-GAN weights are downloaded by torch.hub on first run (~1.4 GB).
# speechbrain is OPTIONAL (preferred for ECAPA-TDNN x-vector but the
# Wav2Vec2 mean-pool fallback is used if speechbrain fails to import).
# Uncomment the next line if your environment supports it:
# speechbrain>=0.5

# --- Part IV: anti-spoofing + adversarial ---------------------------------
scikit-learn>=1.3   # ROC-AUC / EER computation

# --- pipeline + utilities -------------------------------------------------
tqdm>=4.65

# --- audio scraping (Part 0; only needed if you re-fetch source audio) ----
# We supply audio manually in this repo, so these are optional.
# yt-dlp>=2024.4
# av>=11.0
